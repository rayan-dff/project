// 01_patterns_records.dart — patterns and records

// TODO: 1) area(shape):
// - circle(radius: r)
// - rect(w: w, h: h)
// - square(side: s)
// Return area as double using pattern matching on records or sealed classes.
// For now, represent shapes as records: ('circle', r), ('rect', w, h), ('square', s)

void main() {
  // print(area(('circle', 2.0))); // ~12.566
  // print(area(('rect', 3.0, 4.0))); // 12
  // print(area(('square', 5.0))); // 25
}