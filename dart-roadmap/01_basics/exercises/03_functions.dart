// 03_functions.dart — functions and parameters

// TODO: 1) greet(name, {title = 'Mr/Ms'}): returns 'Hello, <title> <name>'.
// TODO: 2) power(base, [exp = 2]): returns base^exp (integer power).
// TODO: 3) makeCounter(start): returns a closure that increments and returns the count each call.

void main() {
  // print(greet('Ada', title: 'Dr'));
  // print(power(3));
  // final next = makeCounter(10);
  // print(next()); // 11
  // print(next()); // 12
}