// 02_streams.dart — Streams

// TODO: 1) numbers(n): Stream<int> that emits 1..n with 10ms intervals.
// TODO: 2) doubleStream(s): transforms a Stream<int> into Stream<int> with each value*2.
// TODO: 3) sumStream(s): consumes the stream and returns Future<int> sum.

Future<void> main() async {
  // final s = numbers(5);
  // final doubled = doubleStream(s);
  // print(await sumStream(doubled)); // 30
}