// 01_futures.dart — Futures and async/await

// TODO: 1) async addAsync(a, b): returns Future<int> after 100ms delay.
// TODO: 2) fetchUser(id): simulate API call: if id < 0 throw, else return 'user-<id>' after 50ms.
// TODO: 3) sumSequential(): await addAsync(1,2), then addAsync(result,3) and return.

Future<void> main() async {
  // print(await addAsync(1, 2));
  // try {
  //   print(await fetchUser(-1));
  // } catch (e) {
  //   print('error: $e');
  // }
  // print(await sumSequential());
}