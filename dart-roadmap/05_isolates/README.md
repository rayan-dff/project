# 05_isolates — Concurrency with isolates

## Topics
- What isolates are; no shared memory, message passing
- `Isolate.spawn`, `ReceivePort`, `SendPort`
- Compute-style patterns

## Do
- Exercises in `exercises/`.