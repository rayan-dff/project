// 01_classes.dart — classes and constructors

// TODO: Create a class Rectangle with width and height (double),
// - default constructor Rectangle(this.width, this.height)
// - getter area
// - method scale(factor) that returns a new Rectangle scaled
// - toString => 'Rectangle(w: <w>, h: <h>)'

void main() {
  // final r = Rectangle(3, 4);
  // print(r.area); // 12
  // print(r.scale(2)); // Rectangle(w: 6.0, h: 8.0)
}