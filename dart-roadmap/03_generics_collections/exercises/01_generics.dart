// 01_generics.dart — generics

// TODO: Implement a generic class Box<T> with a field value of type T.
// - Provide a constructor Box(this.value)
// - Provide a method map<U>(U Function(T) f) that returns Box<U>
// TODO: Implement T firstOf<T>(List<T> items) that returns the first item, throw if empty.

void main() {
  // final b = Box(10).map((n) => n.toString());
  // print(b.value); // '10'
  // print(firstOf(['a', 'b'])); // 'a'
}