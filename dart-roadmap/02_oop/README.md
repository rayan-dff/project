# 02_oop — Object-oriented Dart

## Topics
- Classes, fields, methods
- Constructors: default, named, redirecting, initializer lists
- Getters/setters
- Abstract classes, interfaces (implicit), `implements`
- Inheritance vs composition
- Mixins (`mixin`, `with`)
- Extensions (`extension on Type`)
- Enums (enhanced enums)

## Do
- Implement exercises in `exercises/`.